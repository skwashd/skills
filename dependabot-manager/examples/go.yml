# Go ecosystem
# Drop this entry into the `updates:` block of .github/dependabot.yml

  - package-ecosystem: "gomod"
    directory: "/"
    schedule:
      interval: "daily"
    cooldown:
      default-days: 1
    labels:
      - "dependencies"
      - "go"
