# PHP ecosystem
# Drop this entry into the `updates:` block of .github/dependabot.yml

  - package-ecosystem: "composer"
    directory: "/"
    schedule:
      interval: "daily"
    cooldown:
      default-days: 3
    labels:
      - "dependencies"
      - "php"
